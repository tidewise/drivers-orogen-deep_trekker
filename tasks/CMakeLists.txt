# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(deep_trekkerTaskLib)
ADD_LIBRARY(${DEEP_TREKKER_TASKLIB_NAME} SHARED
    ${DEEP_TREKKER_TASKLIB_SOURCES})
add_dependencies(${DEEP_TREKKER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${DEEP_TREKKER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${DEEP_TREKKER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DEEP_TREKKER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DEEP_TREKKER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${DEEP_TREKKER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${DEEP_TREKKER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DEEP_TREKKER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DEEP_TREKKER_TASKLIB_HEADERS}
    DESTINATION include/orocos/deep_trekker)

